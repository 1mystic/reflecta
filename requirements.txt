# --- core data / science ---
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4

# --- modeling ---
torch>=2.2            # DKT / SAKT (CPU fine to start; GPU on Kaggle/Colab)

# --- experiment tracking (free tier) ---
wandb>=0.16
mlflow>=2.11          # optional local alternative; tracking wrapper supports both

# --- backend API ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- datasets / io ---
datasets>=2.18        # HuggingFace datasets (Eedi, MMLU, etc.)
requests>=2.31
tqdm>=4.66

# --- reflection engine (free/local LLM) ---
# transformers + a small instruct model, or llama-cpp-python for gguf.
# Kept optional so the core installs light; uncomment when working on M4.
# transformers>=4.40
# sentence-transformers>=2.7

# --- dev ---
pytest>=8.0
